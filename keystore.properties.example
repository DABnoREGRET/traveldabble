# Android Release Keystore Configuration Template
# Copy this file to keystore.properties and fill in your secrets.
# NEVER commit keystore.properties or .jks files to version control.

storeFile=composeApp/release.jks
storePassword=your_keystore_password
keyAlias=your_key_alias
keyPassword=your_key_password
